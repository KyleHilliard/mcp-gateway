# =============================================================================
# MCP Gateway — Environment Variables
# Copy to .env and fill in real values. NEVER commit .env to git.
# =============================================================================

# --- Monarch Money ---
MONARCH_EMAIL=your-email@example.com
MONARCH_PASSWORD=your-password
MONARCH_MFA_SECRET=your-mfa-secret

# --- TickTick ---
TICKTICK_CLIENT_ID=your-client-id
TICKTICK_CLIENT_SECRET=your-client-secret
TICKTICK_REDIRECT_URI=http://localhost:8000/callback
TICKTICK_ACCESS_TOKEN=your-access-token

# --- Fabric (future, when Tailscale bridge is configured) ---
# FABRIC_BASE_URL=http://100.86.129.100:8080
